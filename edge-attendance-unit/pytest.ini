[pytest]
# Auto mode lets bare `async def test_*` coroutines run without per-test
# @pytest.mark.asyncio decorators. Pinned pytest-asyncio defaults to strict,
# so this is set explicitly for reproducible runs.
asyncio_mode = auto
testpaths = tests
