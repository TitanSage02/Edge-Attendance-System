# Lightweight dependencies to run the test suite off-device (CI / dev machine).
# Pi-only and heavy native libs (board, spidev, RPi.GPIO, picamera2, cv2,
# insightface, chromadb, ...) are stubbed in tests/conftest.py when missing.
#
# Versions are pinned for reproducible CI runs (Python 3.10).
pydantic==1.10.26
paho-mqtt==2.1.0
numpy==1.26.4
aiohttp==3.10.11
aiofiles==24.1.0
python-dotenv==1.0.1
matplotlib==3.9.4
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-mock==3.14.0
pytest-cov==6.0.0
pytest-timeout==2.3.1
